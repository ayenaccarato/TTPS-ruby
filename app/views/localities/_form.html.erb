<div class="row">
  <div class="col-log-12">
    <div class="card p-5"> 
      <%= form_with(model: locality) do |form| %>
        <% if locality.errors.any? %>
          <div style="color: red">
            <h2><%= pluralize(locality.errors.count, "error") %> prohibited this locality from being saved:</h2>

            <ul>
              <% locality.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div>
          <%= form.label :province, style: "display: block" %>
          <%= form.select :province, options_for_select(get_provinces, locality.province) %>
        </div>

        <div>
          <%= form.label :name, style: "display: block" %>
          <%= form.text_field :name %>
        </div>

        <div>
          <%= form.submit %>
        </div>
      <% end %>
    </div>
  </div>
</div>
