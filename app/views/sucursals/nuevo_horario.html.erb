<h1>Agregar horario</h1>
<div class="row">
    <div class="col">
        <div class="card">
            <table class="table-info">
                <tr>
                <td><b>Nombre de la sucursal:</b></td>
                <td><%= @sucursal.nombre %></td>
                </tr>
            </table>
        </div>
    </div>
</div>

<div class="row mt-5">
    <div class="col-sm">
        <div class="card p-5">
            <%= form_with model: @horario, url: crear_horario_sucursal_path(@sucursal) do |f| %>
                
                <% if @horario.errors.any? %>
                    <div class="alert alert-danger alert-dismissible" role="alert">
                        <h3> <%= flash[:notice] %> </h3>
                        <ul>
                            <% @horario.errors.full_messages do |message| %>
                                <li><%= message %></li>
                            <% end %>
                        </ul>
                    </div>
                
                <% end %>
                
                <div class="form-group">
                    <%= f.label :dia, "Día" %>
                    <%= f.select :dia, options_for_select(Horario.get_dias),{}, {class: 'form-control-sm'} %>
                    
                </div>

                <div class="mb-0">
                    <label class="form-label">Example</label>
                    <input class="form-control form-control-solid" placeholder="Pick date rage" id="kt_daterangepicker_2"/>
                </div>
                
                <div class="form-group">
                    <%= f.label :desde, "Hora inicio" %>
                   

                </div>

                <div class="form-group">
                    <%= f.label :hasta, "Hora fin" %>


                </div>

                <div class="form-group">
                    <%= f.submit "Agregar", class: 'btn btn-primary' %>
                    <%= link_to "Cancelar", sucursal_path(@sucursal), class: 'btn btn-danger' %>
                </div>

            <% end %>
        </div>
    </div>
</div>
